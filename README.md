# Weather Bauhaus

Frontend + Flask backend for OpenWeather.

## Run backend
1. Create venv and install requirements.
2. Set OPENWEATHER_API_KEY in backend/.env
3. Run: python backend/app.py

## Run frontend
Open frontend/index.html in a browser or use a simple static server.